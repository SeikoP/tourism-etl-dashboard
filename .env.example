# Airflow Environment Configuration
# Set the user ID for Airflow containers (important for file permissions)
AIRFLOW_UID=50000

# Airflow Image Configuration
# Use 'latest' for newest version, or specify version like '2.10.2' for stability
AIRFLOW_IMAGE_NAME=apache/airflow:latest

# Project Directory (default: current directory)
AIRFLOW_PROJ_DIR=.

# Admin User Configuration (optional - will be created during init)
_AIRFLOW_WWW_USER_USERNAME=bungmoto
_AIRFLOW_WWW_USER_PASSWORD=0946932602Aa

# Additional PIP Requirements (leave empty for production)
_PIP_ADDITIONAL_REQUIREMENTS=